import React, { Component } from 'react';
import './Preview.css';

class Preview extends Component {
  render() {
    return (
      <section className="Preview">
        A preview of the selected project
      </section>
    );
  }
}

export default Preview;
